# Optional non-secret overrides for local stack.
# Copy to .env.local if needed.

MASTER_KEY=local-mux-e2e-master-key
OPENCLAW_RAW_STREAM=
OPENCLAW_ANTHROPIC_PAYLOAD_LOG=
# Optional: provide a dedicated local-test WhatsApp auth dir to enable WhatsApp inbound.
# If unset, the stack still runs (WhatsApp inbound disabled).
WA_AUTH_SOURCE=/absolute/path/to/local-test-whatsapp-auth/default

# Optional: shared register key used by OpenClaw instances to register with mux.
MUX_REGISTER_KEY=local-mux-e2e-register-key

# pair-token.sh default TTL (seconds)
TTL_SEC=900
